package com.generic_framework.features;

import com.generic_framework.utils.Status;

public interface SetCustomStatusPage {
    public HomePage setStatus(Status status);
    public HomePage clearStatus();
}
